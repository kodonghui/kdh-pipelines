---
name: One command to rule them all
description: CEO wants ONE command that runs everything overnight, not multiple manual steps
type: feedback
---

사장님은 명령어 여러 개를 순서대로 치는 걸 원하지 않음. 하나만 치면 밤새 알아서 돌아가야 함.

**Why:** "어떤 순서로 저 명령어들을 쳐야하는지 어케알아?" + "나 자러 갔을 때 알아서 작업 다 해놓으면 좋겠는데"

**How to apply:** `/kdh-go` 하나로 모든 걸 자동 판단+실행. 개별 스킬(build, review, sprint 등)은 내부용. 밤새 실행 = `/kdh-go 계속` (GATE 자동 진행). 사장님한테 6개 명령어 외우라고 하지 말 것.
